<%= form_for @event, :url => event_path(@event), :html => { :role => "form" }  do |f| %>

    <%= render :partial => 'form', :locals => { :f => f } %>

    <%= f.submit "Update", :class => "btn btn-primary" %>
<% end %>