<%= form_for @event, :url => events_path, :html => { :role => "form" }  do |f| %>

    <%= render :partial => 'form', :locals => { :f => f } %>

    <%= f.submit "Create", :class => "btn btn-primary" %>
<% end %>